patch:
  # reset值为0，切换输入法时默认中文输出
  # reset值为1，切换输入法时默认英文输出
  "switches/@0/reset": 1
  "key_binder/bindings":
    - { when: composing, accept: Return, send: Escape }
    - { when: has_menu, accept: semicolon, send: 2 }
    - { when: has_menu, accept: apostrophe, send: 3 }
